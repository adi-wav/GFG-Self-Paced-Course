It is useful to save to space used up in recursion call stack
Quicksort can also be written like:
void quicksort(int a[], int l, int h){
    Begin:
    iif(l<r){
        int pi=partition(a,l,r);
        quicksort(a,l,p);
        l=p+1;
        goto Begin;
    }
}